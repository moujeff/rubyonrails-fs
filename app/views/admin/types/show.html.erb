<p style="color: green"><%= notice %></p>

<%= render @type %>

<div>
  <%= link_to "Edit this type", edit_admin_type_path(@type) %> |
  <%= link_to "Back to types", admin_types_path %>

  <%= button_to "Destroy this type", admin_types_path(@type), method: :delete %>
</div>
