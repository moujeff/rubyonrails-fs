<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      <%= link_to "Show this category", admin_categories_path(category) %>
    </p>
  <% end %>
</div>

<%= link_to "New category",new_admin_category_path %>
